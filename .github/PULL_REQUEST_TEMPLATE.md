## Summary

<!-- What does this change and why? -->

## Verification

- [ ] `pnpm typecheck && pnpm lint && pnpm schema:check && pnpm test && pnpm build` passes
- [ ] No tests spawn the `codex` CLI
- [ ] If grading/caching semantics changed: `RUNNER_VERSION` bumped and noted below

## Notes

<!-- Anything reviewers should know: semantic changes, cache invalidation, follow-ups. -->
